name: dropdown_button2_examples
description: Examples for DropdownButton2

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dropdown_button2:

dependency_overrides:
  dropdown_button2:
    path: ../

dev_dependencies:
  flutter_lints: ^2.0.1
